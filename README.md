# SKU Catalog

A simple SKU catalog implementation in Python.

## Usage

1. Create a `SKUCatalog` instance.
2. Upload a CSV file containing SKU metadata using the `upload_csv` method.
3. Retrieve a SKU by its ID using the `get_sku` method.

## Testing

Run the tests using `pytest`.
